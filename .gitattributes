# Normalize line endings. Source files are LF in the repo and on every
# checkout (incl. Windows CI), so tools like gofmt behave consistently.
* text=auto eol=lf

# Code, specs, config, docs -> LF
*.go    text eol=lf
*.sh    text eol=lf
*.mod   text eol=lf
*.sum   text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.ts    text eol=lf
*.tsx   text eol=lf

# Windows-native scripts keep CRLF
*.ps1   text eol=crlf
*.bat   text eol=crlf
*.cmd   text eol=crlf

# Binaries (never normalize)
*.png   binary
*.ico   binary
*.exe   binary
*.dll   binary
*.ipa   binary
*.zip   binary
*.plist binary
